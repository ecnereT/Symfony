# To get started with security, check out the documentation:
# http://symfony.com/doc/current/book/security.html

security:
    encoders:
        FOS\UserBundle\Model\UserInterface: bcrypt

    role_hierarchy:
        ROLE_COMMENTATOR: ROLE_COMMENTATOR
        ROLE_CONTRIBUTOR: [ROLE_COMMENTATOR, ROLE_CONTRIBUTOR]

    providers:
        fos_userbundle:
            id: fos_user.user_provider.username

    firewalls:
        main:
            pattern: ^/
            form_login:
                provider: fos_userbundle
                csrf_token_generator: security.csrf.token_manager
                # if you are using Symfony < 2.8, use the following config instead:
                # csrf_provider: form.csrf_provider

            logout:       true
            anonymous:    true

    access_control:
        - { path: ^/login$, role: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/register, role: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/resetting, role: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/admin/, role: ROLE_ADMIN }
        - { path: ^/post/new, roles: [ROLES_CONTRIBUTOR, ROLE_ADMIN, ROLE_USER] }
        - { path: ^/post/[0-9]+/edit, roles: [ROLES_CONTRIBUTOR, ROLE_ADMIN, ROLE_USER] }
        - { path: ^/post/[0-9]+/delete, roles: [ROLES_CONTRIBUTOR, ROLE_ADMIN] }